# Assets

Place optional presentation images, exported icons, or company-approved visual assets here. The current deck uses CSS-generated industrial visuals and CDN icons, so no binary assets are required.
